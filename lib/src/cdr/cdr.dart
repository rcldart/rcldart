// src/cdr/cdr.dart — vendored ros_cdr: runtime schema-driven decoding of ANY
// ROS 2 message via the rosidl introspection typesupport (no codegen).
// Vendored into rcldart so the package is self-contained and pub.dev-publishable.
library;

export 'introspection.dart';
export 'dynamic_message.dart';
